<%= form_with(model: menu_master, local: true) do |form| %>
  <% if menu_master.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(menu_master.errors.count, "error") %> prohibited this menu_master from being saved:</h2>

      <ul>
      <% menu_master.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :menu_ID %>
    <%= form.number_field :menu_ID %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price %>
  </div>

  <div class="field">
    <%= form.label :price_tax_not %>
    <%= form.number_field :price_tax_not %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
