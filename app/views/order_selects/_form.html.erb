<%= form_with(model: order_select, local: true) do |form| %>
  <% if order_select.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order_select.errors.count, "error") %> prohibited this order_select from being saved:</h2>

      <ul>
      <% order_select.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :order_ID %>
    <%= form.number_field :order_ID %>
  </div>

  <div class="field">
    <%= form.label :menu_ID %>
    <%= form.number_field :menu_ID %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
