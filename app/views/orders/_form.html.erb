<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :order_ID %>
    <%= form.number_field :order_ID %>
  </div>

  <div class="field">
    <%= form.label :menu_ID %>
    <%= form.number_field :menu_ID %>
  </div>

  <div class="field">
    <%= form.label :table_ID %>
    <%= form.number_field :table_ID %>
  </div>

  <div class="field">
    <%= form.label :login_ID %>
    <%= form.text_field :login_ID %>
  </div>

  <div class="field">
    <%= form.label :total %>
    <%= form.number_field :total %>
  </div>

  <div class="field">
    <%= form.label :total_tax_not %>
    <%= form.number_field :total_tax_not %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.check_box :status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
